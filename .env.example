# Base URL for axios client. If you use the Vite dev proxy (recommended), leave this empty.
VITE_API_BASE_URL=

# When running `npm run dev`, Vite will proxy /api to this backend target.
# Example: http://localhost:3000
VITE_API_PROXY_TARGET=
